<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Dialog -->
    <style name="zdjdialog_general" parent="@android:style/Theme.Dialog">
        <item name="android:textSize">15sp</item>
        <item name="android:backgroundDimEnabled">true</item>        　
        <item name="android:windowIsTranslucent">true</item>        　
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">false</item>
    </style>

    <style name="zdjer_dialog" parent="@android:style/Theme.Holo.Light.Dialog.MinWidth">
        <item name="android:textColor">@color/zdjer_blue</item>
        <item name="android:textSize">15sp</item>
        <!-- 设置未浮动窗口 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 设置无边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 设置无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置完全透明 -->
        <item name="android:windowBackground">@color/zdjer_transparent_half</item>
        <!-- 设置屏幕变暗 -->
    </style>

    <!--<style name="dialogTheme" parent="@android:style/android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/zdjer_bg1</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>-->

    <!-- 加载滚动条样式 -->
    <style name="zdjer_loading">
        <item name="android:indeterminate">true</item>
        <item name="android:indeterminateDrawable">@drawable/zdjer_progress_loading</item>
        <item name="android:indeterminateDuration">1000</item>
        <item name="android:indeterminateOnly">true</item>
    </style>

</resources>
